models:
  Models/Room:
    room_number: integer
    is_clean: boolean
    max_guests: integer
    roomtype_id: id foreign

  Models/Roomtype:
    name: string
    price: integer
    room_surface: integer
    relationships:
      hasMany: Room

seeders: Room, Roomtype
